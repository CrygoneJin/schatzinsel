{
  "permissions": {
    "allow": [
      "Bash(*)"
    ],
    "deny": [
      "Bash(rm -rf:*)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /home/user/schatzinsel && git fetch origin 2>&1 | tail -3 && echo '✅ git fetch done'",
            "timeout": 15,
            "statusMessage": "git fetch origin..."
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "npx tsc --noEmit || echo '{\"decision\":\"block\",\"reason\":\"Typ-Fehler gefunden. npm run typecheck zum Debuggen.\"}'",
            "timeout": 30,
            "statusMessage": "Typecheck..."
          }
        ]
      }
    ]
  }
}
